# Only the label AFTER the slash in AW-18434780037/your_label.
# Set these in the hosting build environment, then rebuild/deploy.
VITE_GOOGLE_ADS_FORM_SUBMIT_LABEL=
# Optional: separate conversion action for visiting the contact page.
VITE_GOOGLE_ADS_CONTACT_VIEW_LABEL=
